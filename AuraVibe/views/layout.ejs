<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
    <script
      src="https://code.jquery.com/jquery-3.7.0.min.js"
      integrity="sha256-2Pmvv0kuTBOenSvLm6bvfBSSHrUJ+3A7x6P5Ebd07/g="
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="/css/layout.css" />
    <link rel="stylesheet" href="/css/header.css" />
    <link rel="stylesheet" href="/css/footer.css" />

    <%- style %>
  </head>
  <body>
    <%- include('_header') %>
    <!-- using flash which is setup in custom middleware (inside config folder) as locals and send to res -->

    <!-- this below is variable part of the website which will be loaded from other pages. -->

    <main id="layout-main"><%- body %></main>

    <!-- <h1>Hello from layout</h1> -->
    <!-- <h3>
        This is the Layout of the website. There is variable part above
      </h3> -->

    <%- include('_footer') %>

    <script>
      //noty notification for success message.
        <% if (flash.success && flash.success.length > 0){ %>
          new Noty(
            { theme : "sunset",
              text: "<%= flash.success %>",
              type : "success",
              layout: "topRight",
              timeout: 1500, }).show();
      <% } %>
      //noty notification for error message.
      <% if (flash.error && flash.error.length > 0){ %>
          new Noty(
            { theme : "sunset",
              text: "<%= flash.error %>",
              type : "error",
              layout: "topRight",
              timeout: 1500, }).show();
      <% } %>
    </script>
  </body>
</html>
